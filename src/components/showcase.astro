---
import ContentSection from "~/components/content-section.astro";
import ShowcaseCard from "~/components/showcase-card.astro";
import { getShowcase } from "~/data/showcase";

const sites = await getShowcase();
---

<ContentSection title="Recomendaciones" id="showcase">
  <Fragment slot="lead">
    Debido a que seguramente me <span class="text-primary">extrañes </span>
    porque lamentablemente genero eso en las personas, te recomiendo algunos juegos
    <span class="text-primary">cooperativos </span>
  </Fragment>
  <div class="max-w-6xl space-y-2">
    <div class="grid grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-3">
      {sites.map((site) => <ShowcaseCard site={site} />)}
    </div>
    <p class="text-right text-sm">
      <a class="text-primary" href="https://store.steampowered.com/tags/es/Cooperativos/?l=spanish">
        ...más &rarr;
      </a>
    </p>
  </div>
</ContentSection>
